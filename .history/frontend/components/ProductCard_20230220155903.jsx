import { View, Text } from 'react-native';
import React from 'react';

const ProductCard = ({ stock, name, price, image, id, addToCardHandler }) => {
  return (
    <View>
      <Text>ProductCard</Text>
    </View>
  );
};

export default ProductCard;
